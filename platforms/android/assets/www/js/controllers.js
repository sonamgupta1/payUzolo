var appControllers = angular.module('starter.controllers', []).controller('packageController',function($scope,$http,
                                                                                                       serverConfig,$state) {
});
var appServices = angular.module('starter.services', []);
